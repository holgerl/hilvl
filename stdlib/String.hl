System extend String with insert as
	@ var atIndex = (@.this.atIndex)
	@ var first = (@.this at 0 substringTo (@.atIndex))
	@ var last = (@.this at (@.atIndex) substringTo (@.this length _))
	@.first + (@.argument) + (@.last)