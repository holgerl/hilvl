System extend String with insert as
	@ new atIndex = (@.this.atIndex)
	@ new first = (@.this at 0 substringTo (@.atIndex))
	@ new last = (@.this at (@.atIndex) substringTo (@.this length _))
	@.first + (@.argument) + (@.last)