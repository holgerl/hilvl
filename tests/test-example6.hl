# new bar = 1
	
# new foo : // the statement in the argument is not evaluated yet
	# set bar = 2
	
# new barBefore = (#.bar)
	
# foo null // this invokes the foo action, and the code is evaluated (null means no argument)

# new barAfter = (#.bar)

# new results =
	#.barBefore
	#.barAfter

//result: [1, 2]